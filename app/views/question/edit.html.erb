<h1>Question</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for question</p>
     <ul>
       <li>Make sure that it should make sense</li>
       <li>Irrelevent Questions are not Valid!! Do not ask such questions!!.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @question do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter your question title" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter question body" %>
       </div>
       <div class="form-group">
         <%= f.check_box :resolved %> Question Resolved
      </div>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>
