<div class="form-group">
    <%= f.label :rates %>
    <%= f.text_field :rates,
                     class: 'form-control',
                     placeholder: "Enter Ratings (separated by commas)",
                     value: rates.map(&:name).join(', ') %>
  </div>
